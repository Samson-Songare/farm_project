import 'package:flutter/material.dart';

class SoilComposition extends StatelessWidget {
  const SoilComposition({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Column(
          children: [],
        ),
      ),
    );
  }
}
